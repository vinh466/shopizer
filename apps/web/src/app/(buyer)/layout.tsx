import { TMainLayout } from '@shopizer/templates';

const MainLayout = ({ children }: { children: React.ReactNode }) => {
  return <TMainLayout>{children}</TMainLayout>;
};

export default MainLayout;
